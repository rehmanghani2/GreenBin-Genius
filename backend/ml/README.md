# ML Models directory

# Place your trained model files here before deployment:
#   cnn_model.h5   – Keras CNN model (13-class waste classifier)
#   yolo_model.pt  – Ultralytics YOLOv8 model (object detection)
#
# These files are excluded from git (.gitignore) because of their
# large size. Upload them directly to your Hugging Face Space
# repository via the web UI or git-lfs.
